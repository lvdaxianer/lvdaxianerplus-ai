# 熔断器配置测试故事线 11-15：页面配置参数测试

> 本文档包含5个故事线，测试熔断器页面配置功能

---

## 故事线 11：页面配置加载验证

### 故事目标
- **核心问题**：验证 Dashboard 页面正确加载熔断器配置
- **目标用户**：运维人员
- **预期产出**：配置参数正确显示在表单中

### 关键节点/里程碑
- **起点**：Dashboard 页面加载
- **中间步骤**：
  1. 查看「熔断器配置」区域
  2. 验证各参数显示正确
- **终点**：enabled、failureThreshold、successThreshold、halfOpenTime 正确显示

### 测试验证步骤
1. 打开 Dashboard
2. 查看熔断器配置表单
3. 验证默认值：enabled=true, failureThreshold=5, successThreshold=2, halfOpenTime=30000

---

## 故事线 12：修改失败阈值并保存

### 故事目标
- **核心问题**：验证修改 failureThreshold 并保存成功
- **目标用户**：运维人员
- **预期产出**：修改后配置生效，状态重置

### 关键节点/里程碑
- **起点**：failureThreshold=5
- **中间步骤**：
  1. 修改 failureThreshold 为 3
  2. 点击保存按钮
  3. 验证 API 返回成功
- **终点**：failureThreshold=3，所有状态重置

### 测试验证步骤
1. 修改 failureThreshold 输入框为 3
2. 点击「保存配置」
3. 验证 Toast 提示成功
4. 验证 API `/api/circuit-breaker` 返回新配置

---

## 故事线 13：修改成功阈值并保存

### 故事目标
- **核心问题**：验证修改 successThreshold 并保存成功
- **目标用户**：运维人员
- **预期产出**：修改后配置生效

### 关键节点/里程碑
- **起点**：successThreshold=2
- **中间步骤**：
  1. 修改 successThreshold 为 5
  2. 点击保存按钮
- **终点**：successThreshold=5

---

## 故事线 14：修改半开等待时间并保存

### 故事目标
- **核心问题**：验证修改 halfOpenTime 并保存成功
- **目标用户**：运维人员
- **预期产出**：修改后配置生效

### 关键节点/里程碑
- **起点**：halfOpenTime=30000ms
- **中间步骤**：
  1. 修改 halfOpenTime 为 60000ms
  2. 点击保存按钮
- **终点**：halfOpenTime=60000ms

---

## 故事线 15：禁用/启用熔断器切换

### 故事目标
- **核心问题**：验证切换熔断器启用状态
- **目标用户**：运维人员
- **预期产出**：切换后状态正确反映

### 关键节点/里程碑
- **起点**：enabled=true
- **中间步骤**：
  1. 选择「关闭」选项
  2. 点击保存按钮
  3. 验证状态变为不工作
- **终点**：enabled=false，熔断器不触发

---

## 测试声明

> **强制要求**：本故事线必须由 Chrome DevTools MCP 进行测试验证。

*Generated by 故事线技能 | Date: 2026-04-24*