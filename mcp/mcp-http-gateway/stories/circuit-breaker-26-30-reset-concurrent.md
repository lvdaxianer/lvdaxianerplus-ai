# 熔断器配置测试故事线 26-30：重置与并发测试

> 本文档包含5个故事线，测试重置功能和并发场景

---

## 故事线 26：全局重置功能验证

### 故事目标
- **核心问题**：验证 POST /api/circuit-breaker/reset 重置所有工具状态
- **目标用户**：开发人员
- **预期产出**：所有工具状态恢复为 CLOSED，计数归零

### 关键节点/里程碑
- **起点**：多个工具处于不同状态（OPEN、HALF_OPEN）
- **中间步骤**：
  1. 调用全局重置 API
  2. 验证所有工具状态
- **终点**：所有工具状态恢复为 CLOSED

### 测试验证步骤
1. 触发多个工具熔断
2. 调用 `POST /api/circuit-breaker/reset` with `{}` body
3. 验证所有工具状态恢复为 CLOSED
4. 验证失败计数归零

---

## 故事线 27：单工具重置功能验证

### 故事目标
- **核心问题**：验证 POST /api/circuit-breaker/reset 重置指定工具状态
- **目标用户**：开发人员
- **预期产出**：指定工具状态恢复，其他工具保持不变

### 关键节点/里程碑
- **起点**：多个工具处于 OPEN 状态
- **中间步骤**：
  1. 调用单工具重置 API
  2. 验证指定工具恢复
  3. 验证其他工具状态不变
- **终点**：指定工具状态为 CLOSED，其他保持 OPEN

### 测试验证步骤
1. 触发 toolA 和 toolB 熔断
2. 调用 `POST /api/circuit-breaker/reset` with `{"toolName":"toolA"}`
3. 验证 toolA 状态恢复为 CLOSED
4. 验证 toolB 状态仍为 OPEN

---

## 故事线 28：配置更新自动重置验证

### 故事目标
- **核心问题**：验证 PUT /api/circuit-breaker 更新配置时自动重置所有状态
- **目标用户**：开发人员
- **预期产出**：配置更新后，所有状态重置为 CLOSED

### 关键节点/里程碑
- **起点**：多个工具处于 OPEN 状态
- **中间步骤**：
  1. 调用 PUT /api/circuit-breaker
  2. 验证所有状态重置
- **终点**：所有工具状态恢复为 CLOSED

### 测试验证步骤
1. 触发多个工具熔断
2. 调用 `PUT /api/circuit-breaker` 更新任意配置
3. 验证所有工具状态恢复为 CLOSED
4. 验证新配置生效

---

## 故事线 29：并发请求状态一致性测试

### 故事目标
- **核心问题**：验证高并发下熔断器状态一致性
- **目标用户**：开发人员
- **预期产出**：并发请求不导致状态混乱

### 关键节点/里程碑
- **起点**：熔断器 CLOSED 状态
- **中间步骤**：
  1. 并发发送10次失败请求
  2. 验证状态一致性
- **终点**：状态正确变为 OPEN

### 测试验证步骤
1. 设置 failureThreshold=5
2. 并发发送10次失败请求
3. 验证状态正确变为 OPEN
4. 验证失败计数正确

---

## 故事线 30：Dashboard 重置按钮功能验证

### 故事目标
- **核心问题**：验证 Dashboard「重置所有状态」按钮正常工作
- **目标用户**：运维人员
- **预期产出**：点击按钮后所有状态重置

### 关键节点/里程碑
- **起点**：Dashboard 页面，多个工具处于 OPEN 状态
- **中间步骤**：
  1. 点击「重置所有状态」按钮
  2. 验证 Toast 提示成功
  3. 验证状态表格更新
- **终点**：所有工具状态恢复为 CLOSED

### 测试验证步骤
1. 打开 Dashboard
2. 触发多个工具熔断
3. 点击「重置所有状态」按钮
4. 验证 Toast 提示「状态重置成功」
5. 验证状态表格显示所有工具为 CLOSED

---

## 测试声明

> **强制要求**：本故事线必须通过 curl 命令和 Chrome DevTools MCP 进行测试验证。

*Generated by 故事线技能 | Date: 2026-04-24*