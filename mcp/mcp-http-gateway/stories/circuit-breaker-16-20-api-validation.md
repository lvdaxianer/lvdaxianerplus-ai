# 熔断器配置测试故事线 16-20：API端点验证

> 本文档包含5个故事线，测试熔断器 API 端点功能

---

## 故事线 16：GET /api/circuit-breaker 端点验证

### 故事目标
- **核心问题**：验证获取熔断器配置 API 正确返回数据
- **目标用户**：开发人员
- **预期产出**：API 返回 config、status、description 字段

### 关键节点/里程碑
- **起点**：服务启动
- **中间步骤**：
  1. 调用 GET /api/circuit-breaker
  2. 验证响应格式
- **终点**：返回正确的配置和状态数据

### 测试验证步骤
```bash
curl -s http://localhost:11112/api/circuit-breaker | jq '.'
```

验证响应包含：
- config.enabled
- config.failureThreshold
- config.successThreshold
- config.halfOpenTime
- status（每个工具的状态）
- description（字段说明）

---

## 故事线 17：PUT /api/circuit-breaker 端点验证

### 故事目标
- **核心问题**：验证更新熔断器配置 API 正常工作
- **目标用户**：开发人员
- **预期产出**：API 更新成功，返回新配置

### 关键节点/里程碑
- **起点**：默认配置
- **中间步骤**：
  1. 调用 PUT /api/circuit-breaker
  2. 发送新配置 JSON
- **终点**：配置更新成功

### 测试验证步骤
```bash
curl -s -X PUT http://localhost:11112/api/circuit-breaker \
  -H 'Content-Type: application/json' \
  -d '{"enabled":true,"failureThreshold":3,"successThreshold":2,"halfOpenTime":20000}' \
  | jq '.'
```

验证响应包含：
- success: true
- message: "熔断器配置已更新，所有状态已重置"
- config（新配置）
- status（重置后的状态）

---

## 故事线 18：POST /api/circuit-breaker/reset 端点验证

### 故事目标
- **核心问题**：验证重置熔断器状态 API 正常工作
- **目标用户**：开发人员
- **预期产出**：重置成功，状态恢复为 CLOSED

### 关键节点/里程碑
- **起点**：熔断器处于 OPEN 状态
- **中间步骤**：
  1. 调用 POST /api/circuit-breaker/reset
- **终点**：状态恢复为 CLOSED

### 测试验证步骤
```bash
curl -s -X POST http://localhost:11112/api/circuit-breaker/reset \
  -H 'Content-Type: application/json' \
  -d '{}' \
  | jq '.'
```

---

## 故事线 19：API 参数验证（无效参数）

### 故事目标
- **核心问题**：验证 API 正确处理无效参数
- **目标用户**：开发人员
- **预期产出**：返回明确的错误信息

### 关键节点/里程碑
- **起点**：正常服务
- **中间步骤**：
  1. 发送 enabled="invalid"（非 boolean）
  2. 发送 failureThreshold=0（小于1）
  3. 发送 halfOpenTime=500（小于1000）
- **终点**：返回 400 错误

### 测试验证步骤
```bash
# 测试无效 enabled
curl -s -X PUT http://localhost:11112/api/circuit-breaker \
  -H 'Content-Type: application/json' \
  -d '{"enabled":"invalid","failureThreshold":5,"successThreshold":2,"halfOpenTime":30000}'

# 测试无效 failureThreshold
curl -s -X PUT http://localhost:11112/api/circuit-breaker \
  -H 'Content-Type: application/json' \
  -d '{"enabled":true,"failureThreshold":0,"successThreshold":2,"halfOpenTime":30000}'

# 测试无效 halfOpenTime
curl -s -X PUT http://localhost:11112/api/circuit-breaker \
  -H 'Content-Type: application/json' \
  -d '{"enabled":true,"failureThreshold":5,"successThreshold":2,"halfOpenTime":500}'
```

---

## 故事线 20：API 单工具重置验证

### 故事目标
- **核心问题**：验证重置单个工具熔断器状态
- **目标用户**：开发人员
- **预期产出**：指定工具状态重置，其他工具保持不变

### 关键节点/里程碑
- **起点**：部分工具处于 OPEN 状态
- **中间步骤**：
  1. 调用 POST /api/circuit-breaker/reset
  2. 指定 toolName
- **终点**：指定工具状态重置

### 测试验证步骤
```bash
curl -s -X POST http://localhost:11112/api/circuit-breaker/reset \
  -H 'Content-Type: application/json' \
  -d '{"toolName":"getUser"}' \
  | jq '.'
```

---

## 测试声明

> **强制要求**：本故事线必须通过 curl 命令和 Chrome DevTools MCP 进行测试验证。

*Generated by 故事线技能 | Date: 2026-04-24*