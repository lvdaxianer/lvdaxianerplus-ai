# 熔断器配置测试故事线 01-10：状态流转测试

> 本文档包含10个故事线，测试熔断器状态流转功能

---

## 故事线 01：初始状态验证

### 故事目标
- **核心问题**：验证熔断器初始状态是否正确显示为 CLOSED
- **目标用户**：运维人员、开发人员
- **预期产出**：Dashboard 显示所有工具熔断器状态为 CLOSED

### 关键节点/里程碑
- **起点**：服务启动，Dashboard 加载
- **中间步骤**：
  1. 访问 Dashboard 熔断器状态表格
  2. 检查每个工具的状态显示
- **终点**：所有工具状态为 CLOSED，失败/成功计数为 0

### 测试验证步骤
1. 打开 Dashboard 页面 `http://localhost:11112/dashboard`
2. 查看「熔断器状态」表格
3. 验证每个工具状态为 CLOSED
4. 验证失败/成功计数为 0

---

## 故事线 02：CLOSED → OPEN 状态流转（failureThreshold=5）

### 故事目标
- **核心问题**：验证连续失败5次触发熔断（CLOSED → OPEN）
- **目标用户**：运维人员
- **预期产出**：连续失败达到阈值后，状态变为 OPEN

### 关键节点/里程碑
- **起点**：熔断器处于 CLOSED 状态
- **中间步骤**：
  1. 配置 failureThreshold=5
  2. 连续触发5次失败请求
  3. 观察状态变化
- **终点**：熔断器状态变为 OPEN

### 测试验证步骤
1. 设置 failureThreshold=5
2. 模拟连续5次失败
3. 验证状态变为 OPEN
4. 验证 Dashboard 显示正确

---

## 故事线 03：CLOSED → OPEN 状态流转（failureThreshold=3）

### 故事目标
- **核心问题**：验证连续失败3次触发熔断（敏感度更高）
- **目标用户**：运维人员
- **预期产出**：连续失败3次后，状态变为 OPEN

### 关键节点/里程碑
- **起点**：熔断器处于 CLOSED 状态
- **中间步骤**：
  1. 配置 failureThreshold=3
  2. 连续触发3次失败请求
  3. 观察状态变化
- **终点**：熔断器状态变为 OPEN

---

## 故事线 04：CLOSED → OPEN 状态流转（failureThreshold=10）

### 故事目标
- **核心问题**：验证连续失败10次触发熔断（敏感度更低）
- **目标用户**：运维人员
- **预期产出**：连续失败10次后，状态变为 OPEN

### 关键节点/里程碑
- **起点**：熔断器处于 CLOSED 状态
- **中间步骤**：
  1. 配置 failureThreshold=10
  2. 连续触发10次失败请求
  3. 观察状态变化（前9次应仍为 CLOSED）
- **终点**：第10次失败后，状态变为 OPEN

---

## 故事线 05：OPEN → HALF_OPEN 状态流转

### 故事目标
- **核心问题**：验证熔断后等待时间到期进入半开状态
- **目标用户**：运维人员
- **预期产出**：halfOpenTime 到期后，状态变为 HALF_OPEN

### 关键节点/里程碑
- **起点**：熔断器处于 OPEN 状态
- **中间步骤**：
  1. 配置 halfOpenTime=20000ms
  2. 触发熔断
  3. 等待20秒
- **终点**：状态自动变为 HALF_OPEN

---

## 故事线 06：HALF_OPEN → CLOSED 状态流转

### 故事目标
- **核心问题**：验证半开状态下成功请求恢复熔断器
- **目标用户**：运维人员
- **预期产出**：连续成功达到阈值后，状态恢复为 CLOSED

### 关键节点/里程碑
- **起点**：熔断器处于 HALF_OPEN 状态
- **中间步骤**：
  1. 配置 successThreshold=2
  2. 连续触发2次成功请求
- **终点**：状态恢复为 CLOSED

---

## 故事线 07：HALF_OPEN → OPEN 状态流转（失败回退）

### 故事目标
- **核心问题**：验证半开状态下失败请求重新熔断
- **目标用户**：运维人员
- **预期产出**：半开状态下失败，状态回退到 OPEN

### 关键节点/里程碑
- **起点**：熔断器处于 HALF_OPEN 状态
- **中间步骤**：
  1. 触发1次失败请求
- **终点**：状态立即回退为 OPEN

---

## 故事线 08：完整状态流转循环

### 故事目标
- **核心问题**：验证完整状态流转闭环
- **目标用户**：运维人员
- **预期产出**：CLOSED → OPEN → HALF_OPEN → CLOSED 完整循环

### 关键节点/里程碑
1. CLOSED → OPEN：连续失败5次
2. OPEN → HALF_OPEN：等待 halfOpenTime
3. HALF_OPEN → CLOSED：连续成功2次

---

## 故事线 09：成功请求重置失败计数

### 故事目标
- **核心问题**：验证 CLOSED 状态下成功请求重置失败计数
- **目标用户**：运维人员
- **预期产出**：成功请求后失败计数归零

### 关键节点/里程碑
- **起点**：失败计数为3（未达阈值）
- **中间步骤**：
  1. 触发1次成功请求
- **终点**：失败计数重置为0

---

## 故事线 10：禁用熔断器测试

### 故事目标
- **核心问题**：验证禁用熔断器后状态始终为 CLOSED
- **目标用户**：运维人员
- **预期产出**：enabled=false 时，熔断器不工作

### 关键节点/里程碑
- **起点**：熔断器配置 enabled=false
- **中间步骤**：
  1. 连续触发多次失败请求
- **终点**：状态始终为 CLOSED，不触发熔断

---

## 测试声明

> **强制要求**：本故事线必须由 Chrome DevTools MCP 进行测试验证。
> 所有故事线节点必须验证通过。

*Generated by 故事线技能 | Date: 2026-04-24*