# 熔断器配置测试故事线 21-25：阈值效果测试

> 本文档包含5个故事线，测试不同阈值设置的效果

---

## 故事线 21：failureThreshold=1 敏感度测试

### 故事目标
- **核心问题**：验证 failureThreshold=1 时，单次失败立即触发熔断
- **目标用户**：运维人员
- **预期产出**：1次失败后状态立即变为 OPEN

### 关键节点/里程碑
- **起点**：熔断器 CLOSED 状态，failureThreshold=1
- **中间步骤**：
  1. 触发1次失败请求
  2. 验证状态变化
- **终点**：状态变为 OPEN

### 测试验证步骤
1. 设置 failureThreshold=1
2. 模拟1次失败
3. 验证状态立即变为 OPEN
4. 验证 Dashboard 显示正确

---

## 故事线 22：failureThreshold=20 容忍度测试

### 故事目标
- **核心问题**：验证 failureThreshold=20 时，需20次失败才触发熔断
- **目标用户**：运维人员
- **预期产出**：前19次失败状态仍为 CLOSED，第20次变为 OPEN

### 关键节点/里程碑
- **起点**：熔断器 CLOSED 状态，failureThreshold=20
- **中间步骤**：
  1. 触发19次失败请求
  2. 验证状态仍为 CLOSED
  3. 触发第20次失败
- **终点**：状态变为 OPEN

### 测试验证步骤
1. 设置 failureThreshold=20
2. 模拟19次失败，验证状态仍为 CLOSED
3. 模拟第20次失败，验证状态变为 OPEN

---

## 故事线 23：successThreshold=1 快速恢复测试

### 故事目标
- **核心问题**：验证 HALF_OPEN 状态下 successThreshold=1 时，单次成功立即恢复
- **目标用户**：运维人员
- **预期产出**：1次成功后状态立即恢复为 CLOSED

### 关键节点/里程碑
- **起点**：熔断器 HALF_OPEN 状态，successThreshold=1
- **中间步骤**：
  1. 触发1次成功请求
- **终点**：状态恢复为 CLOSED

### 测试验证步骤
1. 触发熔断进入 OPEN 状态
2. 等待 halfOpenTime 进入 HALF_OPEN
3. 设置 successThreshold=1
4. 触发1次成功，验证立即恢复 CLOSED

---

## 故事线 24：successThreshold=10 保守恢复测试

### 故事目标
- **核心问题**：验证 HALF_OPEN 状态下 successThreshold=10 时，需10次成功才恢复
- **目标用户**：运维人员
- **预期产出**：前9次成功状态仍为 HALF_OPEN，第10次恢复 CLOSED

### 关键节点/里程碑
- **起点**：熔断器 HALF_OPEN 状态，successThreshold=10
- **中间步骤**：
  1. 触发9次成功请求
  2. 验证状态仍为 HALF_OPEN
  3. 触发第10次成功
- **终点**：状态恢复为 CLOSED

### 测试验证步骤
1. 触发熔断进入 OPEN 状态
2. 等待进入 HALF_OPEN
3. 设置 successThreshold=10
4. 触发9次成功，验证仍为 HALF_OPEN
5. 触发第10次成功，验证恢复 CLOSED

---

## 故事线 25：halfOpenTime=5000ms 快速探测测试

### 故事目标
- **核心问题**：验证 halfOpenTime=5000ms 时，5秒后快速进入探测状态
- **目标用户**：运维人员
- **预期产出**：熔断后5秒自动进入 HALF_OPEN

### 关键节点/里程碑
- **起点**：熔断器 OPEN 状态，halfOpenTime=5000ms
- **中间步骤**：
  1. 等待5秒
- **终点**：状态变为 HALF_OPEN

### 测试验证步骤
1. 设置 halfOpenTime=5000ms
2. 触发熔断进入 OPEN
3. 等待5秒
4. 验证状态变为 HALF_OPEN

---

## 测试声明

> **强制要求**：本故事线必须通过 curl 命令和 Chrome DevTools MCP 进行测试验证。

*Generated by 故事线技能 | Date: 2026-04-24*